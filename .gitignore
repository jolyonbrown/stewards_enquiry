# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
.env.*

# AgentCore CLI state is governed by agentcore/.gitignore (which deliberately
# commits agentcore/.cli/deployed-state.json — see docs/DEVIATIONS.md #4)

# Build artefacts
dist/
*.zip

# Runtime output (regenerated on every triage run)
proposals.jsonl

# Node cruft from the CLI tooling
node_modules/
